var tty = require('tty.js');

var app = tty.createServer({
    shell: 'bash',
      users: {
            dshaw: 'voxer'
    },
      port: 8889
});

app.get('/dshaw', function(req, res, next) {
    res.send('voxer');
});

app.listen();
